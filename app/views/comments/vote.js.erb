<% if @vote.valid? %>
$('#comment_<%= @comment.id %>_votes').html('<%= @comment.total_votes %> Votes');
<% else %>
  alert("<%= @current_user.username %>, you can only vote for the '<%= @comment.body %>' comment one time.");
<% end %>