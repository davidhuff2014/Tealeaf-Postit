<% if @vote.valid? %>
  $('#post_<%= @post.slug %>_votes').html('<%= @post.total_votes %> Votes');
<% else %>
  // alert("<%= @current_user.username %>, you can only vote for the '<%= @post.title %>' post one time.");
  $('#post_vote_error_<%= @post.slug %>').show()
<% end %>